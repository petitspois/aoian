"use client"

import * as React from "react"
import ConversationHeader from './header'

const Conversation = () => {
  return (
    <section className={'grow bg-layout-background'}>
      <ConversationHeader />
    </section>
  )
}

export default Conversation;
